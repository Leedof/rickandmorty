import { Box } from '@mui/material';
import { CharacterInfo } from 'features';

export const Character = () => (
  <Box sx={{ pt: { xs: 4, sm: 10 }, pb: { xs: 10, sm: 31.5 } }}>
    <CharacterInfo />
  </Box>
);
